{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev src/index.ts --port 3000",
    "build": "tsc",
    "test": "node -e \"const fs=require('node:fs'),path=require('node:path'),cp=require('node:child_process');const root=process.argv[1]||'src';const out=[];const walk=(d)=>{if(!fs.existsSync(d))return;for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(e.isFile()&&(p.endsWith('.test.ts')||p.endsWith('.spec.ts')))out.push(p.split(path.sep).join('/'));}};walk(root);out.sort();if(!out.length){console.error('No test files found under '+root+' (*.test.ts or *.spec.ts)');process.exit(1);}const child=cp.spawn(process.execPath,['--import','tsx','--test',...out],{stdio:'inherit'});child.on('error',(err)=>{console.error('Failed to run Node test runner:',err);process.exit(1);});child.on('exit',(code,signal)=>{if(signal){process.kill(process.pid,signal);return;}process.exit(code??1);});\"",
    "test:controllers": "node -e \"const fs=require('node:fs'),path=require('node:path'),cp=require('node:child_process');const root=process.argv[1]||'src';const out=[];const walk=(d)=>{if(!fs.existsSync(d))return;for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(e.isFile()&&(p.endsWith('.test.ts')||p.endsWith('.spec.ts')))out.push(p.split(path.sep).join('/'));}};walk(root);out.sort();if(!out.length){console.error('No test files found under '+root+' (*.test.ts or *.spec.ts)');process.exit(1);}const child=cp.spawn(process.execPath,['--import','tsx','--test',...out],{stdio:'inherit'});child.on('error',(err)=>{console.error('Failed to run Node test runner:',err);process.exit(1);});child.on('exit',(code,signal)=>{if(signal){process.kill(process.pid,signal);return;}process.exit(code??1);});\" src/controllers",
    "test:controllers:coverage": "c8 --reporter=text-summary --reporter=lcov --check-coverage --lines=80 --functions=80 --branches=70 --statements=80 --all --src=src --include=src/controllers/**/*.ts --exclude=src/controllers/**/*.test.ts --exclude=src/controllers/**/*.spec.ts node -e \"const fs=require('node:fs'),path=require('node:path'),cp=require('node:child_process');const root=process.argv[1]||'src';const out=[];const walk=(d)=>{if(!fs.existsSync(d))return;for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(e.isFile()&&(p.endsWith('.test.ts')||p.endsWith('.spec.ts')))out.push(p.split(path.sep).join('/'));}};walk(root);out.sort();if(!out.length){console.error('No test files found under '+root+' (*.test.ts or *.spec.ts)');process.exit(1);}const child=cp.spawn(process.execPath,['--import','tsx','--test',...out],{stdio:'inherit'});child.on('error',(err)=>{console.error('Failed to run Node test runner:',err);process.exit(1);});child.on('exit',(code,signal)=>{if(signal){process.kill(process.pid,signal);return;}process.exit(code??1);});\" src/controllers",
    "start": "node dist/index.js",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "db:push": "prisma db push",
    "db:seed": "ts-node prisma/seed.ts",
    "prisma:generate": "prisma generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@prisma/adapter-pg": "^7.4.2",
    "@prisma/client": "^7.4.2",
    "@sendgrid/mail": "^8.1.6",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "express-rate-limit": "^8.3.1",
    "jsonwebtoken": "^9.0.3",
    "midtrans-client": "^1.4.3",
    "minio": "^8.0.7",
    "multer": "^2.1.0",
    "nodemailer": "^8.0.1",
    "pg": "^8.19.0",
    "prisma": "^7.4.2",
    "socket.io": "^4.8.1",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1",
    "yamljs": "^0.3.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/midtrans-client": "^1.4.0",
    "@types/multer": "^2.0.0",
    "@types/node": "^25.3.3",
    "@types/nodemailer": "^7.0.11",
    "@types/pg": "^8.18.0",
    "@types/swagger-jsdoc": "^6.0.4",
    "@types/swagger-ui-express": "^4.1.8",
    "nodemon": "^3.1.14",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  }
}
